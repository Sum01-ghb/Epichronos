name: Keep Backend Alive

on:
  schedule:
    - cron: "*/14 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Backend
        run: |
          echo "Pinging backend..."
          curl https://epichronos-server.onrender.com
